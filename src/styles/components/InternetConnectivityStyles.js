import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    width: '100%',
    paddingLeft: 16,
    paddingRight: 16,
    marginTop: 10,
  },
  errorHolder: {
    borderRadius: 20,
    backgroundColor: '#fff',
    width: '100%',
    padding: 16,
  },
  error: {
    color: 'red',
    fontSize: 14,
  },
});
